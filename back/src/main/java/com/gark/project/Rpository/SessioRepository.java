package com.gark.project.Rpository;

import com.gark.project.Entity.Session;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SessioRepository extends JpaRepository<Session, Long> {
}
