
<h2>liste de classroom</h2>
<table border="2">
    <thead>
        <tr>
            <th>ID</th>
            <th>name</th>
            <th>Editer</th>
            
        
        </tr>
    </thead>
    <tbody>
{%for f in p %}
 <tr>
                    <td>{{ f.id}}</td>
                    <td>{{ f.name }}</td>
                   <td><a href="{{ path('modifierclass', {'id': f.id, 'name': f.name}) }}">Editer</a></td>
                   <td><a href="{{ path('supprimerclass', {'id': f.id}) }}">supprimer</a></td>

                </tr>

{%endfor%}
  </tbody>
</table>